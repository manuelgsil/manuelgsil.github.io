---
---

<div class="hero">
  <div class="hero-icons" aria-hidden="true">
    <svg style="position: absolute; left: 1%; top: 2%; width: 76px; animation: bailoteo 6.4s ease-in-out infinite"><use href="#ico-tostadora" /></svg>
    <svg style="position: absolute; right: 1%; top: 1%; width: 70px; animation: brinco 4.8s ease-in-out infinite 0.3s"><use href="#ico-perro" /></svg>
    <svg style="position: absolute; left: 13%; bottom: 9%; width: 88px; animation: vueltas 14s linear infinite"><use href="#ico-disquete" /></svg>
    <svg style="position: absolute; right: 11%; bottom: 7%; width: 112px; animation: vaiven 5.6s ease-in-out infinite"><use href="#ico-php" /></svg>
    <svg style="position: absolute; left: 1%; top: 47%; width: 84px; animation: latido 4.2s ease-in-out infinite"><use href="#ico-cafe" /></svg>
    <svg style="position: absolute; right: 1%; top: 44%; width: 100px; animation: flota 8.2s ease-in-out infinite"><use href="#ico-libro" /></svg>
    <svg style="position: absolute; left: 30%; top: 2%; width: 70px; animation: flota 7.1s ease-in-out infinite 1.1s"><use href="#ico-cafe" /></svg>
    <svg style="position: absolute; right: 28%; bottom: 1%; width: 76px; animation: bailoteo 5.1s ease-in-out infinite 0.7s"><use href="#ico-disquete" /></svg>
  </div>

  <h1>Manuel's Web Page</h1>
  <p class="tagline">Desarrollador · PHP y Laravel · Sevilla</p>
  <div class="links">
    <a class="btn btn-light" href="mailto:manuelguillen93@gmail.com">Escríbeme</a>
    <a class="btn btn-dark" href="https://linkedin.com/in/manuelguillensilva" target="_blank" rel="noopener">LinkedIn</a>
    <a class="btn btn-light" href="tel:661539038">661 539 038</a>
    <a class="btn btn-dark" href="/cv-manuel-guillen.pdf" download>Descargar CV</a>
  </div>
</div>

<style>
  .hero {
    background: var(--ac);
    border-bottom: 4px solid var(--ink);
    padding: 60px 20px 54px;
    text-align: center;
    position: relative;
    overflow: hidden;
    min-height: 420px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  h1 {
    margin: 0;
    position: relative;
    font-family: Georgia, "Times New Roman", serif;
    font-weight: 700;
    font-size: clamp(40px, 7.5vw, 78px);
    line-height: 1;
    letter-spacing: -2px;
    color: var(--ink);
    text-shadow: 3px 3px 0 var(--paper);
  }

  .tagline {
    margin: 22px 0 0;
    position: relative;
    font-size: 16px;
    font-weight: 700;
    letter-spacing: 1px;
    background: var(--ink);
    color: var(--ac);
    padding: 8px 16px;
    display: inline-block;
  }

  .links {
    margin: 26px 0 0;
    position: relative;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
  }

  .btn {
    font-size: 14px;
    font-weight: 700;
    text-decoration: none;
    border: 3px solid var(--ink);
    padding: 10px 16px;
    transition: transform 0.15s, box-shadow 0.15s;
  }

  .btn-light {
    color: var(--ink);
    background: var(--paper);
    box-shadow: 4px 4px 0 var(--ink);
  }

  .btn-light:hover {
    transform: translate(2px, 2px);
    box-shadow: 2px 2px 0 var(--ink);
  }

  .btn-dark {
    color: var(--ac);
    background: var(--ink);
    box-shadow: 4px 4px 0 var(--paper);
  }

  .btn-dark:hover {
    transform: translate(2px, 2px);
    box-shadow: 2px 2px 0 var(--paper);
  }

  @media (max-width: 640px) {
    .hero-icons {
      display: none;
    }
  }
</style>
